# Minimal requirements - ComfyUI installs to network volume at runtime
runpod>=1.7.0
boto3>=1.34
huggingface_hub>=0.24
hf_transfer>=0.1.8
requests>=2.32
torch>=2.0.0

# ComfyUI dependencies (in case network volume install is incomplete)
sqlalchemy>=2.0
aiohttp>=3.9
aiosqlite>=0.20
